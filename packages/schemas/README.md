# Shared schemas

Released schemas are immutable. Add a new version instead of changing the meaning of
an existing field. Python and TypeScript code both validate representative fixtures
against these files.

